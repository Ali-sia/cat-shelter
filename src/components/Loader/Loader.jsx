import { LoaderDiv } from './Loader.styled';
import { ContainerWidth } from '../App/App.styled';

const Loader = () => {
  return (
    <ContainerWidth>
      <LoaderDiv></LoaderDiv>;
    </ContainerWidth>
  );
};

export default Loader;
